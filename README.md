# Discock - Веб-мессенджер

Discock - это веб-версия мессенджера наподобие Discord, где пользователи из разных городов могут общаться в реальном времени через комнаты (каналы).

## 🚀 Технологии

- **Backend:** Flask 3.0
- **Real-time:** Flask-SocketIO (WebSocket)
- **База данных:** PostgreSQL / SQLite (для разработки)
- **Аутентификация:** Flask-Login
- **Frontend:** HTML, CSS, JavaScript + Socket.IO

## 📋 Возможности

- ✅ Регистрация и авторизация пользователей
- ✅ Создание и управление комнатами (каналами)
- ✅ Обмен сообщениями в реальном времени через WebSocket
- ✅ Отображение города пользователя
- ✅ Адаптивный дизайн
- ✅ История сообщений

## 🛠 Установка и запуск локально

### 1. Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd Discock
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=ваш-секретный-ключ-случайная-строка
DATABASE_URL=sqlite:///discock.db
PORT=5000
```

Для генерации SECRET_KEY:
```python
import secrets
print(secrets.token_hex(32))
```

### 5. Инициализация базы данных

```bash
python app.py
```

При первом запуске автоматически создастся база данных и общая комната "Общий чат".

### 6. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 🌐 Деплой на Amvera Cloud

### 1. Подготовка проекта

1. Убедитесь, что все файлы закоммичены в Git
2. Создайте репозиторий на GitHub
3. Запушьте код в репозиторий

### 2. Настройка Amvera

1. Зайдите на [cloud.amvera.ru](https://cloud.amvera.ru/projects)
2. Создайте новый проект типа "Приложение"
3. Подключите ваш GitHub репозиторий
4. Выберите ветку для деплоя (обычно `main` или `master`)

### 3. Настройка базы данных

1. В панели Amvera создайте PostgreSQL базу данных
2. Скопируйте URL подключения (DATABASE_URL)

### 4. Переменные окружения

В настройках приложения на Amvera добавьте переменные окружения:

- `SECRET_KEY` - секретный ключ Flask (сгенерируйте случайную строку)
- `DATABASE_URL` - URL PostgreSQL базы данных от Amvera
- `PORT` - обычно устанавливается автоматически

### 5. Деплой

После настройки Amvera автоматически соберёт и задеплоит ваше приложение при пуше в основную ветку GitHub.

### 6. Проверка

Откройте URL вашего приложения, предоставленный Amvera, и проверьте работу приложения.

## 📁 Структура проекта

```
Discock/
├── app.py                 # Главный файл Flask приложения
├── models.py              # Модели базы данных
├── forms.py               # Формы для аутентификации
├── requirements.txt       # Зависимости Python
├── amvera.yaml           # Конфигурация для Amvera
├── .gitignore            # Игнорируемые файлы Git
├── README.md             # Документация
├── templates/            # HTML шаблоны
│   ├── base.html
│   ├── login.html
│   ├── register.html
│   └── chat.html
└── static/               # Статические файлы
    ├── css/
    │   └── style.css
    └── js/
        └── chat.js
```

## 🔧 Настройка для продакшена

### Безопасность

1. **Обязательно** измените `SECRET_KEY` на случайную строку:
   ```python
   import secrets
   secrets.token_hex(32)
   ```

2. Используйте PostgreSQL вместо SQLite для продакшена

3. Настройте HTTPS через Amvera (обычно настраивается автоматически)

### Масштабирование

- Для большей нагрузки можно увеличить количество worker'ов в `amvera.yaml`:
  ```yaml
  run: eventlet -w 4 --bind 0.0.0.0:$PORT app:app
  ```

## 📝 API Endpoints

- `GET /` - Главная страница (редирект)
- `GET /login` - Страница входа
- `POST /login` - Авторизация
- `GET /register` - Страница регистрации
- `POST /register` - Регистрация нового пользователя
- `GET /chat` - Страница чата
- `GET /logout` - Выход
- `GET /api/rooms` - Список комнат
- `POST /api/rooms` - Создание комнаты
- `GET /api/messages/<room_id>` - Сообщения комнаты

### WebSocket Events

- `connect` - Подключение к серверу
- `join_room` - Присоединение к комнате
- `leave_room` - Выход из комнаты
- `send_message` - Отправка сообщения
- `new_message` - Получение нового сообщения

## 🐛 Решение проблем

### Проблемы с WebSocket на Amvera

Если WebSocket не работает, проверьте:
1. Настройки транспорта в `chat.js` (должны быть `['websocket', 'polling']`)
2. Поддержку WebSocket на вашем тарифе Amvera

### Проблемы с базой данных

1. Убедитесь, что `DATABASE_URL` правильно настроен
2. Для PostgreSQL проверьте формат URL (должен начинаться с `postgresql://`)
3. Проверьте права доступа к базе данных

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 👨‍💻 Разработка

Для разработки используйте SQLite (по умолчанию), для продакшена - PostgreSQL на Amvera.

### Запуск с миграциями (опционально)

Если хотите использовать Flask-Migrate:

```bash
flask db init
flask db migrate -m "Initial migration"
flask db upgrade
```

## 🎨 Особенности дизайна

Дизайн вдохновлён Discord с тёмной темой и современным UI. Интерфейс адаптивный и работает на разных устройствах.

---

Удачи с деплоем! 🚀
