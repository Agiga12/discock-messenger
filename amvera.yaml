# Конфигурация для деплоя на Amvera Cloud
runtime: python
version: 3.11

# Правильная команда запуска через gunicorn
run: gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT app:app

# Переменные окружения (DATABASE_URL и SECRET_KEY) настройте в панели Amvera